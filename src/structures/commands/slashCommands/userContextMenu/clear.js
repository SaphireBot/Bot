export default {
    name: 'Apagar 50 Mensagens',
    dm_permission: false,
    type: 3,
    async execute({ interaction: interaction, client: client, emojis: e }) {
        
        // const { targetMessage } = interaction

        // const user = targetMessage.author
        
        // const messages = await targetMessage.channel.messages.fetch()
        // const messagesToDelete = messages.filter(msg => msg.author.id === user.id)

        // console.log(messagesToDelete.length)

        // return await interaction.reply({
        //     content: `--- ${user?.tag}`
        // })

    }
}